# Introduction

A user is supposed to be a person having access to workspaces.

From a tech perspective a user is a set of devices. A user can have multiple devices. A device is a set of credentials and the content is encrypted per device.

There is one device, which in the code is referred to as `mainDevice` that has special meaning. This device is used
