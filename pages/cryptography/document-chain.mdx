# Document Chain

// create a chain
// id

// add-share-device
// runs createDocumentShareLink
// returns virtualDeviceKey
// stores in the chain the
// - signingPublicKey
// - encryptionPublicKey
// - encryptionPublicKeySignature
// - expiresAt
// - role
// can only be done by someone with editor role in the workspace

// remove-share-device
// removes it from the chain

// each chain state must reference the workspace chain event to define the members
// TODO how to define the devices
